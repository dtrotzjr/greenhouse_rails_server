<%= form_for(@image_datum) do |f| %>
  <% if @image_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_datum.errors.count, "error") %> prohibited this image_datum from being saved:</h2>

      <ul>
      <% @image_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :filename %><br>
    <%= f.text_area :filename %>
  </div>
  <div class="field">
    <%= f.label :data_point_id %><br>
    <%= f.text_field :data_point_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
